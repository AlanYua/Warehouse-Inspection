import type { Metadata } from "next";

export const metadata: Metadata = { title: "通路/廠商主檔" };

export default function ChannelsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
